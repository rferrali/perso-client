import { BvalcssDirective } from './bvalcss.directive';

describe('BvalcssDirective', () => {
  it('should create an instance', () => {
    const directive = new BvalcssDirective();
    expect(directive).toBeTruthy();
  });
});
